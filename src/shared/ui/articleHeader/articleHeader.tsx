import {Card, CardBody,} from "@nextui-org/card";

export default function ArticleHeader(){
    return (
        <Card className="max-w-[700px] m-auto">
          <CardBody>
            <p className="text-center">pockemon weight</p>
          </CardBody>
        </Card>
      );
}